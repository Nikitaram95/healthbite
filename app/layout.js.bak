// app/layout.tsx
import '../styles/globals.css';

export const metadata = {
  title: 'Healtbite',
  description: 'Приложение с категориями постов и комментариями',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  );
}